<Window x:Class="EmailToastUI.ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Email Toast" Height="125" Width="380"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Storyboard x:Key="FadeInSlideUp">
            <DoubleAnimation Storyboard.TargetName="MainGrid" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ThicknessAnimation Storyboard.TargetName="MainGrid" Storyboard.TargetProperty="Margin" From="20,0,-20,0" To="0,0,0,0" Duration="0:0:0.4">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
        <Storyboard x:Key="FadeOutSlideDown" Completed="FadeOut_Completed">
            <DoubleAnimation Storyboard.TargetName="MainGrid" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ThicknessAnimation Storyboard.TargetName="MainGrid" Storyboard.TargetProperty="Margin" From="0,0,0,0" To="20,0,-20,0" Duration="0:0:0.3">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid Name="MainGrid" Margin="20,0,-20,0" Opacity="0" MouseLeftButtonUp="Grid_MouseLeftButtonUp" MouseEnter="MainGrid_MouseEnter" MouseLeave="MainGrid_MouseLeave" Cursor="Hand">
        <Border Background="#1A1C20" CornerRadius="12" Margin="10,10,10,10" BorderThickness="1" BorderBrush="#2C3038">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="25" ShadowDepth="15" Opacity="0.5" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Border x:Name="GlowBorder" CornerRadius="12" Opacity="0.08" Background="#EA4335" />
                <Grid Margin="16,14,16,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border x:Name="IconBorder" Width="44" Height="44" Margin="0,0,16,0" CornerRadius="10" VerticalAlignment="Top" Background="#EA4335">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="6" ShadowDepth="2" Opacity="0.2"/>
                        </Border.Effect>
                        <TextBlock x:Name="IconText" Text="G" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,2"/>
                    </Border>

                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel Grid.Row="0" Margin="0,0,0,4">
                            <TextBlock x:Name="AppNameTextBlock" Text="Gmail" Foreground="#A9A9B0" FontSize="11" FontWeight="Bold" DockPanel.Dock="Left" VerticalAlignment="Center" TextOptions.TextFormattingMode="Display"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" DockPanel.Dock="Right">
                                <TextBlock x:Name="TimeTextBlock" Text="now" Foreground="#7A7A80" FontSize="11" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <Button x:Name="CloseButton" Width="16" Height="16" Background="Transparent" BorderThickness="0" Click="CloseButton_Click" Cursor="Hand" ToolTip="Dismiss" VerticalAlignment="Center" Opacity="0" Padding="0">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#33FFFFFF"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                    <Path Data="M0,0 L8,8 M8,0 L0,8" Stroke="#A9A9B0" StrokeThickness="1.5" Width="8" Height="8" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>
                            </StackPanel>
                        </DockPanel>

                        <TextBlock Grid.Row="1" x:Name="SenderTextBlock" Text="Sender Name" Foreground="#F0F0F5" FontSize="13" FontWeight="Bold" TextTrimming="CharacterEllipsis" Margin="0,0,0,2"/>
                        <TextBlock Grid.Row="2" x:Name="SubjectTextBlock" Text="Subject" Foreground="#D2D2D8" FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,0,0,2"/>

                        <StackPanel Grid.Row="3" Margin="0,0,10,0">
                            <TextBlock x:Name="PreviewTextBlock" Text="Email preview content..." Foreground="#98989FA0" FontSize="12" TextTrimming="CharacterEllipsis" Margin="0,1,0,0" TextWrapping="Wrap" MaxHeight="36" />
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
